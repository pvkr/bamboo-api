---
version: 2
plan:
  project-key: QW2
  name: Plan from YAML
  key: LAUNCH

stages:
  - First Build Stage:
      jobs: 
        - Build Ship Server

Build Ship Server:
  tasks:
    - script:
        - echo 'Hello world'
